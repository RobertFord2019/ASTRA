# Git attributes: enforce LF line endings and set executable bits for scripts.
*.sh     text eol=lf executable        # Enforce LF line endings and set executable bit for shell scripts
*.py     text eol=lf                   # Enforce LF line endings for Python files
